---
## Play
- hosts: all 
  tasks:
          - name: Install nmap
            apt:
                    name: nmap
                    state: present
          - name: Install vim 
            apt: 
                    name: vim 
                    state: present
# Play
- hosts: role_hosting
  tasks:
          - name: Install apache2
            apt:
                    name: apache2
                    state: present
# Play
- hosts: app_hosting
  tasks: 
        - name: Create directory /tmp/toto
          file:
                  path: /tmp/toto/
                  state: directory

